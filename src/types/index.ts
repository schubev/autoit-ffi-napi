export { Hwnd } from './hwnd'

export enum MouseButton {
  Default = '',
  Left = 'left',
  Middle = 'middle',
  Right = 'right',
  Primary = 'primary',
  Main = 'main',
  Secondary = 'secondary',
  Menu = 'menu',
}

export enum SendMode {
  Default = 0,
  Raw = 1,
}
