// This file is generated by src/codegen/function-gen.ts.
import { MouseButton } from '../../types'
import { PromisifiedAutoitLib } from '../../generated-lib-type'
import { inWstrOfString } from '../../wrap-utils'

export async function mouseClickDrag(
  button: MouseButton,
  xStart: number,
  yStart: number,
  xEnd: number,
  yEnd: number,
  speed = 10,
  library?: Pick<PromisifiedAutoitLib, 'AU3_MouseClickDrag'>,
): Promise<boolean> {
  const buttonBuffer = inWstrOfString(button)
  const lib = library ?? (await import('../../default-lib')).lib
  const result = await lib.AU3_MouseClickDrag(
    buttonBuffer,
    xStart,
    yStart,
    xEnd,
    yEnd,
    speed,
  )
  return result !== 0
}
