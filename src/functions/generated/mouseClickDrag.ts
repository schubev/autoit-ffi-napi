// This file is generated by src/codegen/function-gen.ts.
import { MouseButton } from '../../types'
import { inWstrOfString } from '../../wrap-utils'
import { lib } from '../../lowlevel'
import { promisify } from 'util'

const AU3_MouseClickDrag = promisify(lib.AU3_MouseClickDrag.async)

export async function mouseClickDrag(
  button: MouseButton,
  xStart: number,
  yStart: number,
  xEnd: number,
  yEnd: number,
  speed = 10,
): Promise<boolean> {
  const buttonBuffer = inWstrOfString(button)
  const result = await AU3_MouseClickDrag(
    buttonBuffer,
    xStart,
    yStart,
    xEnd,
    yEnd,
    speed,
  )
  return result !== 0
}
