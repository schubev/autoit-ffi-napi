// This file is generated by src/codegen/function-gen.ts.
import { Hwnd } from '../../types'
import { lib } from '../../lowlevel'
import { outWstrOfSize, outWstrResolver } from '../../wrap-utils'

export async function controlGetFocusByHandle(
  window: Hwnd,
  controlSize = 512,
): Promise<string> {
  const outBuffer = outWstrOfSize(controlSize)
  return new Promise(resolve => {
    lib.AU3_ControlGetFocusByHandle.async(
      window,
      outBuffer,
      controlSize,
      outWstrResolver(outBuffer, resolve),
    )
  })
}
