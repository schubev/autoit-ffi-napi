// This file is generated by src/codegen/function-gen.ts.
import { WindowDescription } from 'autoit-advanced-descriptor'
import { inWstrOfString, inWstrOfWindowDescription, outWstrOfSize } from '../../wrap-utils'
import { lib } from '../../lowlevel'

async function controlGetFocus(windowDescription: string | WindowDescription, windowText: string, controlSize = 512): Promise<void> {
  const windowDescriptionBuffer = inWstrOfWindowDescription(windowDescription)
  const windowTextBuffer = inWstrOfString(windowText)
  const outBuffer = outWstrOfSize(controlSize)
  return new Promise(resolve => {
    lib.AU3_ControlGetFocus.async(windowDescriptionBuffer, windowTextBuffer, outBuffer, controlSize, resolve)
  })
}