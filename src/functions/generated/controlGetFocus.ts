// This file is generated by src/codegen/function-gen.ts.
import { PromisifiedAutoitLib } from '../../generated-lib-type'
import { WindowDescription } from 'autoit-advanced-descriptor'
import {
  inWstrOfString,
  inWstrOfWindowDescription,
  outWstrOfSize,
  stringOfOutWstr,
} from '../../wrap-utils'

export async function controlGetFocus(
  windowDescription: WindowDescription,
  windowText: string,
  controlSize = 512,
  library?: Pick<PromisifiedAutoitLib, 'AU3_ControlGetFocus'>,
): Promise<string> {
  const windowDescriptionBuffer = inWstrOfWindowDescription(windowDescription)
  const windowTextBuffer = inWstrOfString(windowText)
  const outBuffer = outWstrOfSize(controlSize)
  const lib = library ?? (await import('../../default-lib')).lib
  await lib.AU3_ControlGetFocus(
    windowDescriptionBuffer,
    windowTextBuffer,
    outBuffer,
    controlSize,
  )
  return stringOfOutWstr(outBuffer)
}
