// This file is generated by src/codegen/function-gen.ts.
import { SendMode } from '../../types';
import { inWstrOfString } from '../../wrap-utils';
import { lib } from '../../lowlevel';
import { promisify } from 'util';
const AU3_Send = promisify(lib.AU3_Send.async);
export async function send(text, mode = SendMode.Default) {
    const textBuffer = inWstrOfString(text);
    await AU3_Send(textBuffer, mode);
}
